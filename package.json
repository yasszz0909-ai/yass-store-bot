{
  "name": "yass-store-bot",
  "version": "2.0.0",
  "description": "Discord top-up store bot (Free Fire, Mobile Legends, Roblox) with ticket & post-claim payment system.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "lint": "node -c index.js && node -e \"const fs=require('fs');fs.readdirSync('./src/systems').filter(f=>f.endsWith('.js')).forEach(f=>require('./src/systems/'+f))\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.0"
  }
}
